language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mongoose' ]; then cd .. && eval "mv $currentfolder generator-mongoose" && cd generator-mongoose; fi

